spring:
  application:
    name: smart-interview
  mail:
    host: smtp.gmail.com
    port: 587
    username: talel.bm17@gmail.com
    password: gwkyvjhjpfhyeqdo
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  datasource:
    url: jdbc:postgresql://localhost:5432/smart_interview_db
    username: postgres
    password: tl1212
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

server:
  port: 8080

servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

app:
  upload:
    dir: uploads/cvs

